# Requirements for madoc - Independent KAN Research Repository
# This file includes all dependencies needed to run madoc experiments
#
# Installation:
#   pip install -r requirements.txt
#
# Note: PyKAN is NOT included in requirements.txt - it's imported locally via sys.path
# All madoc Python files automatically add the parent directory to sys.path to access pykan

# ============================================================================
# CORE: Dependencies for madoc experiments
# ============================================================================
# NOTE: PyKAN is imported locally via sys.path from the parent directory
# This allows madoc to use the latest pykan code without installing it as a package

# PyTorch - Deep learning framework (CPU version)
# For GPU version on Gadi, use: torch==2.2.2+cu118 with --extra-index-url
torch==2.2.2

# Core scientific computing
numpy==1.24.4
scipy>=1.10.0

# Symbolic mathematics (for KAN's symbolic layer)
sympy==1.11.1

# Visualization
matplotlib==3.6.2
seaborn>=0.12.0

# Data handling
pandas==2.0.1

# Progress bars
tqdm==4.66.2

# Configuration files
pyyaml>=6.0

# ============================================================================
# MACHINE LEARNING: Required for section2_new ensemble/clustering
# ============================================================================
scikit-learn==1.1.3

# ============================================================================
# OPTIMIZERS: Additional optimization algorithms
# ============================================================================
# Levenberg-Marquardt optimizer for PyTorch (required for section2 LM tests)
torch-levenberg-marquardt>=0.1.0

# ============================================================================
# DEVELOPMENT & UTILITIES
# ============================================================================
# For saving/loading results
setuptools==65.5.0

# ============================================================================
# OPTIONAL: Uncomment if needed
# ============================================================================
# For Jupyter notebooks (if running interactive examples)
# jupyter>=1.0.0
# ipykernel>=6.0.0

# For experiment tracking (if using tensorboard)
# tensorboard>=2.10.0

# For evolutionary algorithms in section2_new (alternative to custom implementation)
# deap>=1.3.0

# GPU-specific PyTorch (uncomment for Gadi GPU nodes)
# --extra-index-url https://download.pytorch.org/whl/cu118
# torch==2.2.2+cu118

# ============================================================================
# VERSION NOTES
# ============================================================================
# Python requirement: >=3.6 (recommended: 3.9-3.11)
# PyKAN version: Imported locally from ../pykan/ (parent directory)
# Compatible with NCI Gadi modules: python3/3.10.4
